shows the date, time, patient, and doctor for that particular appointment.
Note that there should NOT be an `Appointment#index` page
–– we don't want to have all of the appointments on display for anyone to see.


<h1> Appointment Information </h1>
<div>
    <%= @appointment.datetime %> <%=link_to @appointment.patient.name, patient_path(@appointment.patient) %> <%=link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %>

</div>